/** @deprecated Dùng `vietnamAddressTree` (dữ liệu từ `vietnam_addresses.json`). Giữ re-export để tránh gãy import cũ. */
export { PROVINCE_NAMES as PROVINCES, getWardNamesByProvinceName } from './vietnamAddressTree';
